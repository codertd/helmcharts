apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rp-ui.fullname" . }}-test-connection"
  labels:
{{ include "rp-ui.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "rp-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
