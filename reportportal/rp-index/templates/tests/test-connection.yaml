apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rp-index.fullname" . }}-test-connection"
  labels:
{{ include "rp-index.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "rp-index.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
