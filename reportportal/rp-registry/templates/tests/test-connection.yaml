apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rp-registry.fullname" . }}-test-connection"
  labels:
{{ include "rp-registry.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "rp-registry.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
