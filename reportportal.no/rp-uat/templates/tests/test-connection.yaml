apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rp-uat.fullname" . }}-test-connection"
  labels:
{{ include "rp-uat.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "rp-uat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
