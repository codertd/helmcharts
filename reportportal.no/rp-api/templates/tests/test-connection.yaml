apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rp-api.fullname" . }}-test-connection"
  labels:
{{ include "rp-api.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "rp-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
