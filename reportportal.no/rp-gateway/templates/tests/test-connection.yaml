apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rp-gateway.fullname" . }}-test-connection"
  labels:
{{ include "rp-gateway.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "rp-gateway.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
